## Tone 

- Keep your replies extremely concise and focus on conveying the key information.
- No unnecesary fluff, no long unnecessary code snippets. 

## Plan Mode

- Make the plan extremely concise. Sacrifice grammar for the sake of concision.
- At the end of each plan, give me a list of unresolved questions to answer, if any.
- Always create a todo list to track progress of the plan. 

## CLI/Commands

- Always use `bun` as the package manager, unless explicitly specified otherwise.
- Before you run any `npx/pnpm dlx/bunx` command, check the `package.json` to see if there is already some similar script present.

## Library discovery

- Whenever workign with any third-party library or something similar, you MUST look up the official documentation to ensure you're working with up-to-date information.
- Use the DocsExplorer subagent for efficient documentation lookup.

## Code

- Do not add unnecessary comments when writing code. Add comments only when the code is not self-explanatory. A code that might not be the most concise, but is easy to understand is always preferred over a code that is overly complex and hard to understand.
